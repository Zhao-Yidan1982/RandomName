## RandomName CLI 随机姓名/编号抽取程序

这是一个功能强大且易于使用的 Python 命令行程序，专门用于随机抽取姓名和编号。适用于课堂点名、活动抽奖、随机分组、以及游戏活动等多种场景。

### 抽取模式

程序共支持五种抽取模式：

- **单人抽取模式**：随机抽取一个姓名，适合课堂点名等场景。
- **性别选择模式**：按性别筛选抽取，可指定男性、女性或全部。
- **多人抽取模式**：一次性抽取多个姓名，适合分组或抽奖。
- **单编号抽取模式**：随机抽取一个编号，保护隐私的同时实现随机选择。
- **多编号抽取模式**：一次性抽取多个编号，适用于匿名抽样。

在任何模式下，输入 `exit` 可退出程序，输入 `quit` 可返回主菜单。

### 程序特色

- 智能编码识别：自动检测 UTF-8 和 GBK 编码，无需手动指定。
- 性别筛选功能：支持按性别条件筛选抽取。
- 完善错误处理：对文件不存在、编码错误、数据格式问题提供明确提示。
- 友好循环交互：中文界面，支持连续抽取和模式切换。
- 高效稳定：基于 Python 标准库，无需额外依赖。

### 数据库格式

程序使用纯文本数据库文件，格式如下：

```

```

要求：使用英文逗号分隔，性别列必须为 `男` 或 `女`，文件编码为 UTF-8 或 GBK。

### 程序演示

运行命令：

```bash
python3 ./main.py
```

交互过程示例：

```
1.单人抽取模式
2.性别选择模式
3.多人抽取模式
4.单编号抽取模式
5.多编号抽取模式
请输入您选择的模式编号或输入"exit"退出运行: 3

请选择抽取人数
或输入"exit"退出运行"quit"重新选择模式: 2

张三 李四

请选择抽取人数
或输入"exit"退出运行"quit"重新选择模式: e

error:请输入整数!!
```

### 应用场景

- 课堂教学：随机点名提问，提高学生参与度和注意力。
- 活动抽奖：公平随机抽取获奖者，避免人为操作。
- 随机分组：快速创建随机小组，促进团队协作。
- 游戏活动：随机选择参与者或分配任务。
---
本文由AI生成仅供参考
